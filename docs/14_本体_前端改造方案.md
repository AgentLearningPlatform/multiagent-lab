# eino-multiagent-lab · 本体前端模块改造方案

| 项 | 内容 |
| --- | --- |
| 版本 / 日期 | v0.12 / 2026-09-23 |
| 作者 | 智能体开发助理（方案口径维护方） |
| 状态 | F1~F5 已实施（afe82e6）；REQ-103 模式 A（b4bf2f8）；O6 Fuseki（a1d6ae2）；O11③ MCP 挂载闭环（201ca3e）；REQ-95 版本 diff 前端（9c5b030）；O12 学习中心 v1（7f7be2b）；REQ-96 全量（561e972）；REQ-90/91 P2 补齐（b20a070）；**REQ-75/76 工具链配置页与引导执行（c55730b）——04 §7 P2 序（95→96→103 模式 A→90/91→75/76）全部完成**；**M14 知识库双子模块前端（c3cbe57）——KnowledgePage RAG/GraphRAG 双子页签 + mode 徽标 + 新建库 mode 单选 + 检索 mode/degraded 回显 + 文档 KG 抽取状态徽标**；**白屏修复（33ba9f0）——智能体配置侧边栏 `Form.useWatch` 误置于 `Form.List` 渲染槽（类组件 render 上下文）触发 React #321 非法 hook 调用整树卸载，提升到组件顶层修复；headless Chrome 前后对照复现/验证** → 待主人验收 |
| 依据 | `docs/04_本体_方案设计.md` v0.12（§2 架构、§4.6/§4.8/§4.10）；`docs/03_本体_需求文档.md` v0.9（REQ-84~88、REQ-90~96、REQ-103/104、验收 19/20）；`docs/02_智能体_技术方案设计.md` §10（前端体系） |
| 触发 | ①主人判断两平面未正交（v0.1）；②主人定位澄清：本体模块 = **学习各种本体构建、运行方式的模块**，学习视角须升为一级体验（v0.3）；③主人 IA 新设想：左侧边栏四栏（构建/资产/运行分栏 + 构建路径分类），现有页面降为「自定义构建」流程并保留七阶段前 4 步（v0.3 拍板） |

---

## 1. 现状与问题

### 1.1 现状盘点（2026-09-22 复核）

- 本体模块为单页 `web/src/pages/OntologyPage.tsx`（约 2000 行），信息架构为 **S1~S7 纵向流水线**：S1 本体来源 → S2 编辑 → S3 校验 → S4 可视化 → S5 运行方式 → S6 对外暴露 → S7 对接智能体；左栏本体列表带 S1~S6 完成度圆点（StageDots）。
- 运行方案**以单个本体为中心**（per-本体 S5 段）；S6/S7 内嵌在构建流水线尾部。
- `web/src/api/client.ts` 已**完整覆盖两平面**：`/api/ontologies*`（构建，:8091 反代）、`/api/runtime-profiles*`（运行，:8090 反代），含 start/stop/reload/logs/versions/artifacts/ai-draft/export 等，可全部复用。
- `ChatWindow.tsx` 已按 `runtime_profile_id` 挂载（"本体增强"chip）；guide 注入路由已补（4ebb957）。
- 学习支撑件（REQ-92/93/94）已实现后端与入口（SPARQL 工作台/源码视图/翻译透视），入口藏在方案详情页。
- semantica 独立栏已上线（f0c7502：worker+反代+独立栏+消费链路轻量版），属导航栏独立模块，不在本方案范围。

### 1.2 与主人定位的三处错位（v0.3 重判）

| # | 现状 | 定位要求（主人拍板） | 判定 |
| --- | --- | --- | --- |
| P1 | 流水线 S1~S7 串成一条线，构建与运行混杂 | 过程（构建）/结果（资产）/运行（方式）三栏分立；现有页面降为「自定义构建」流程，**S1~S4 保留，S5~S7 移交本体资产与本体运行模块** | ❌ 信息架构错位 |
| P2 | 运行方案 per-本体绑定、单一列表 | 运行方案全局一等实体，且**按运行方式（引擎）分组为二级模块**（每种运行方式一个侧边栏项） | ❌ 组织方式错位 |
| P3 | 学习视角无一级入口（学习件 REQ-75/76/90/91 全在 P2、引导视图可隐藏） | 学习者打开平台即可见**学习中心**（默认页）：七阶段路径 + 任务卡 + 构建方式对照 + 运行方式对照 | ❌ 定位错位（v0.3 核心） |

> 结论：功能覆盖面已够（REQ-90~96 学习件已登记），问题在**信息架构把学习视角放逐到末位**。本次改造 = IA 重组 + 学习中心一级化，"重排为主、重写为辅"。

---

## 2. 改造目标与不动项

**目标 IA（一句话）**：本体导航项内部改为**左侧边栏四栏**——**学习中心**（默认页）/ **本体构建**（按构建路径分二级模块）/ **本体资产**（统一仓库管理）/ **本体运行**（按运行方式分二级模块）；顶部导航六模块结构与 semantica 独立栏不变。

**不动项（本次不改）**：
- 后端 API 契约与双反代（`/api/ontologies*`、`/api/runtime-profiles*`、`/api/ontology-llm/generate`）；
- `client.ts` 现有 API 封装（全部复用，仅增量）；
- 主平台顶部导航六模块结构；semantica 独立栏及其零侵入定位（D-O10）；
- antd 6 + 现有样式体系（`pages.css` 的 `onto-*` 类）；
- `ChatWindow` 的 `runtime_profile_id` 挂载与 `ontology.query / ontology.unavailable` 事件渲染。

---

## 3. 现有代码去向映射（v0.3 重排）

| 现有块（OntologyPage.tsx） | 去向 | 改造量 |
| --- | --- | --- |
| S1 本体来源卡（导入 / AI / 内置示例 / 空白） | **自定义构建**流程第 1 步（保留） | 小 |
| S2 编辑 / S3 校验 | **自定义构建**流程第 2/3 步（保留） | 小 |
| S4 可视化（ReactFlow @xyflow/react v12） | **自定义构建**流程第 4 步（保留） | 小 |
| 左栏本体列表 + StageDots | **本体资产**栏资产列表；dots 语义改为 S1~S4 构建段完成度 | 中 |
| S5 运行方式段（方案 CRUD/启停/日志） | **迁移并全局化** → 本体运行栏（按引擎分组，见 §5.4） | 中 |
| S6 对外暴露（facade 端点 + 4 工具表） | 运行方案详情「facade 与工具」Tab（随方案） | 小 |
| S7 对接智能体（挂载指引 + SPARQL 控制台 + 翻译透视） | 运行方案详情「查询与挂载」Tab | 中 |
| 「本体平面服务未启动」降级提示 | 构建 / 资产 / 运行各栏保留对应反代可达性提示 | 小 |

---

## 4. 目标信息架构与导航（D-O11）

```
本体（顶部导航项不变）
└── 左侧边栏（四栏）
    ├── 学习中心（默认页，REQ-104）
    │   ├── 七阶段学习路径主视图（S1→S7 路径条 + 方法论卡片 + 任务卡 + 打卡进度）
    │   ├── 构建方式对照（同一示例本体 × 五路径卡）
    │   └── 运行方式对照（引擎差异卡 + 推理对照入口，随 Fuseki 激活）
    ├── 本体构建
    │   ├── 自定义构建           # 现有页面主体：S1~S4 四步（七阶段前 4 步）
    │   ├── OntoChat 流程        # 对话式 CQ 引导（REQ-103 模式 A 载体）
    │   ├── semantica 流程       # 入口卡 → 跳转 semantica 独立栏（D-O10 零侵入不破）
    │   ├── OntoExtend 流程      # 引导卡先行（方法论+论文指引），工程化登记需求池
    │   └── Open Ontologies 流程 # 双轨引导 + 产物回流（REQ-78 互通后顺畅，P1 手工导入）
    ├── 本体资产                  # 全部已构建本体统一管理（原仓库列表 + 详情工作区）
    │   └── 详情工作区 Tabs：Spec 编辑 / 校验 / 版本（含源码视图 REQ-93）/ 产物 / 可视化 / TTL 导出
    └── 本体运行                  # 按运行方式分二级模块（REQ-84~86 数据层已支持）
        ├── Oxigraph             # 该引擎的方案列表 / 新建向导 / 方案详情
        ├── Fuseki               # O6 交付后点亮；推理对照入口（REQ-94）
        ├── Open Ontologies      # 引导页（独立托管，非 managed 引擎，跳 oo-worker 工作台）
        └── Cayley               # P2 置灰占位（D-O5 v0.4）
```

- 路由沿用 `page` 状态机：`page='ontology'` 内部以 `sidebarKey: 'learn' | 'build' | 'assets' | 'runtime'` 切换；`build` 内以 `buildPath: 'custom' | 'ontochat' | 'semantica' | 'ontoextend' | 'oo'` 驱动路径页；`runtime` 内以 `engineKey` 驱动引擎分组页，再以方案 id 驱动详情。若后续引入路由，按 `/ontology/learn|build|assets|runtime` 映射。
- 文件拆分建议：

```
web/src/pages/ontology/
  OntologyModule.tsx        # 壳：左侧边栏 + 四栏切换 + 降级探测
  LearnPage.tsx             # 学习中心（默认页）
  BuildPage.tsx             # 构建栏：路径导航 + CustomFlow / PathGuideCard×4
  AssetsPage.tsx            # 资产栏：列表 + 详情工作区
  RuntimePage.tsx           # 运行栏：引擎分组 + 方案向导/详情
  components/               # StagePathBar / TaskCardList / OntologyList / OntologyEditor /
                            # ImportWizard / AiDraftWizard / SpecGraph / PathStatusTag /
                            # ProfileWizard / ProfileDrawer / SparqlConsole / TraceTable
```

---

## 5. 页面设计

### 5.1 学习中心（LearnPage，默认页，REQ-104）

- **七阶段学习路径主视图**：S1→S7 横向路径条（数据源 = REQ-74 七阶段模型），每阶段节点展开：方法论卡片入口（`seeds/learning/methodology/`，REQ-90）、任务卡列表（目标/步骤/验收问题/打卡，REQ-91）、工具入口跳转（跳构建/运行/资产栏对应功能，不重复造功能）。
- **构建方式对照区**：五张路径卡（自定义构建 / OntoChat / semantica / OntoExtend / Open Ontologies），每卡标注：适用场景、学习要点、当前状态徽标（可用 / 部分可用 / 引导先行）、示例本体关联（REQ-91 四示例映射四路径）。走完一条任务卡点亮一枚。
- **运行方式对照区**：引擎差异卡（Oxigraph 无推理 / Fuseki 带推理 / Open Ontologies 物化推理+39 工具），推理对照入口在此露出（REQ-94，随 O6 Fuseki 激活；P1 显示"随 Fuseki 交付"置灰）。
- **进度视图**：按阶段聚合任务完成度（REQ-91 ④），打卡状态存 checklist（`task:` key 空间，REQ-76 机制复用）。
- 内容资产（REQ-90/91）**提前至 P1 尾交付**（D-O11）：方法论卡片五模块 + 2 个示例本体建模说明先行，4 示例随 P2 补齐；学习中心界面骨架不依赖 REQ-75 配置页，先跑固定默认工具链。

### 5.2 本体构建（BuildPage，按路径分二级模块）

- **二级导航**：五条路径各一个菜单项；每项右侧状态徽标（可用/部分可用/引导先行）。
- **自定义构建（可用，现有页面主体）**：保留七阶段前 4 步——S1 本体来源（导入/AI 创建/内置示例/空白四入口）→ S2 编辑（JSON 编辑器，图形化 REQ-71 后续接入）→ S3 校验 → S4 可视化（ReactFlow）；**S5~S7 卡片移除**（运行看本体运行栏、facade/挂载看运行方案详情）。步骤间自动流转，完成后引导跳转本体资产栏查看产物、本体运行栏部署。
- **OntoChat 流程（部分可用，REQ-103 模式 A）**：对话式多轮 CQ 引导生成——第一步列能力问题 → 逐轮补全领域信息 → 生成 spec_json 草稿 → 校验循环回喂 → 预览入库；复用 `/api/ontology-llm/generate`（多轮上下文为 REQ-103 模式 A 增量，P2 前段交付；交付前该页显示"对话式引导排期中"+现有单轮 AI 创建引导跳转）。参考 OntoChat 论文三部曲（登记簿 T1）。
- **semantica 流程（入口卡）**：说明卡（semantica 定位/多源摄取能力/与主线边界）+「前往 semantica 独立栏」跳转按钮。**不把 semantica 能力重收进构建平面**（保持 D-O10 零侵入）。
- **OntoExtend 流程（引导先行）**：方法论引导卡（对话式扩展现有本体是什么、参考论文、与 fork/REQ-82 的关系）+ 手工路径指引（导入已有本体 → AI 创建扩展 → 校验）。工程化登记需求池（P2/P3 评估）。
- **Open Ontologies 流程（引导 + 回流）**：引导卡（oo 能力面/双轨定位）+「前往 Open Ontologies 工作台」跳转；产物回流指引（导出 TTL → 主线导入，REQ-78 双轨互通 P2 后自动化）。

### 5.3 本体资产（AssetsPage，统一管理）

- **资产列表**：名称、来源徽标（导入/AI/示例/空白/自定义构建）、形态徽标（original TTL/OWL、spec_json）、版本号、构建段完成度 dots（S1~S4）、被 N 套方案引用（只读）、更新时间。
- **操作**：新建（引导跳转构建栏对应路径）、编辑、浏览、删除（Popconfirm 保留"级联删除 Spec/产物；已启动的运行方案不受影响"——REQ-87）。
- **详情工作区 Tabs**：Spec 编辑（JSON + 保存）｜校验（错误表 + 运行校验）｜版本历史（列表 + 任一版 original 源码查看 = REQ-93）｜产物清单｜可视化（ReactFlow）｜导出 TTL（自建本体入 SPARQL 型方案前置）。
- 保存成功固定提示："已保存新版本；运行方案需在其页面显式重载后生效"（REQ-87）。
- **不出现**：任何引擎、端口、启停配置（正交红线）。

### 5.4 本体运行（RuntimePage，按运行方式分组）

- **二级导航** = 运行方式清单：Oxigraph（可用）｜Fuseki（O6 后点亮）｜Open Ontologies（引导页）｜Cayley（P2 置灰）。数据源 = `listRuntimeProfiles()` 按 engine 过滤，无新后端。
- **引擎分组页（Oxigraph/Fuseki 共用模板）**：
  - 方案卡片：名称、引擎徽标、已加载本体 chips（多值）、状态五态徽标、端口/端点、健康摘要、最近错误（Tooltip 全文）。
  - 行操作：启动 / 停止 / 重载（REQ-87 显式重载）/ 编辑配置 / 删除（二次确认）/ 查看详情。
  - 页顶全局条：**引擎可用性预检条**（last_error 关键词映射友好文案，如 `executable not found` → 安装/容器指引）；**降级条**（:8090 不可达整页只读）。
  - **新建/编辑向导（Steps 三步，REQ-85）**：①运行时类型（本分组内固定为该引擎，P1 仅 Oxigraph 可选、Fuseki disabled 至 O6）；②勾选本体集合（多选，含"需先导出 TTL"引导）；③细节参数（名称/端口/端点可写/推理开关——Fuseki 推理开关激活，Oxigraph 灰置）。配置变更需重启提示。
  - **方案详情 Drawer**：概览（状态时间线/端点/配置）｜日志｜facade 与工具｜查询与挂载（挂载指引全局口径 + SPARQL 工作台 REQ-92 + 翻译透视 REQ-94）｜推理对照入口（Fuseki 分组显示，随 O6）。
- **Open Ontologies 分组页**：引导页——oo 定位（独立托管双轨/能力面/与两平面边界）、oo-worker 工作台跳转、数据边界说明（oo 数据不进主线仓库，回流路径指引）。非 managed 引擎，无启停管理。

---

## 6. 数据与状态约定

- API 全部复用 `client.ts`；增量仅两处——①方案详情"挂载对话"Tab 待后端反查接口（无则先隐藏）；②Manager `/healthz` engines 段已登记 P3 待办（03 v0.6 D-O9），未提供前前端按 last_error 关键词映射。
- 刷新策略：运行分组页方案列表 5s 轮询（或手动刷新 + 启停后立即刷）；资产/构建页按需加载。
- 学习中心打卡：复用 REQ-76 checklist 机制（`task:` / `tool:` 双 key 空间），P1 尾先落 `task:` 空间（任务打卡），`tool:` 打卡随 REQ-75 配置页（P2）。
- 三栏解耦检查：构建栏不请求 `/api/runtime-profiles*`；资产栏只读展示"被引用"徽标不做运行操作；运行栏不调用任何写仓库接口。

---

## 7. 对话挂载联动

- `ChatWindow` "本体增强" chip 数据源 = `listRuntimeProfiles()` 过滤 running（跨引擎全量，不受运行栏分组影响）；挂载、`ontology.query`、`ontology.unavailable` 事件渲染不变（验收 7）。

---

## 8. 实施步骤（可分派、可独立验收）

| 阶段 | 内容 | 出口条件 |
| --- | --- | --- |
| **F1 IA 侧边栏骨架 + 正交化** | OntologyModule 壳（四栏侧边栏）；现有页面主体迁入"自定义构建"并裁剪 S5~S7；资产栏独立（列表 + 详情工作区 Tabs）；S5 方案管理全局化迁入运行栏 Oxigraph 分组 | 四栏可切换；自定义构建无任何运行配置；运行分组页可独立完成"新建方案（多本体）→ 启动 → 停止 → 删除"；tsc/vite 通过 |
| **F2 运行栏完整化** | 引擎分组导航（四项）；方案详情 Drawer（日志/facade/SPARQL 工作台/翻译透视/挂载指引迁移）；预检条 + last_error 友好化；Fuseki/Cayley 置灰占位；oo 引导页 | 验收 7/8 可走通；运行方式对照在运行栏可见 |
| **F3 学习中心 v1（REQ-104 + REQ-90/91 P1 尾档）** | 七阶段路径条 + 任务卡列表 + 打卡 + 进度聚合；构建方式对照六路径卡（状态徽标，D-O14 第六路径引导态）；运行方式对照卡（推理对照已随 O6 激活）；`seeds/learning/` 方法论五模块卡片 + 2 示例建模说明随 seed 交付 | 学习中心为默认页可完整浏览；任务打卡后进度反映 |
| **F4 构建路径二级模块** | OntoChat 流程页（REQ-103 模式 A 交互骨架，后端多轮接口就绪前显示排期态）；semantica/OntoExtend/Open Ontologies 引导卡 ×3；五路径状态徽标 | 构建栏五路径可导航；引导卡内容完整、跳转可用 |
| **F5 收尾** | REQ-93 源码视图入口复核（版本 Tab 内）；REQ-90 CQ 引导输入框（AI 创建第一步）；验收 19/20 对照自查 | REQ-92/93/94 + REQ-104 P1 档全绿 |

> F1 是核心诉求（侧边栏 + 三栏正交），单独交付评审；F3 学习中心为本次定位落地的标志性交付，紧随 F1~F2；F4 依赖 REQ-103 模式 A 后端排期，可先交引导卡部分。

---

## 9. 验收对照（03 需求文档验收条目）

| 验收 | 场景 | 本方案落点 |
| --- | --- | --- |
| 6 | 两套方案并存、分别挂载互不影响 | 运行栏 Oxigraph 分组多卡并存；向导多选本体集合；对话 chip 选方案 |
| 7 | 停止方案 → 挂载对话明确降级，其余不受影响 | 方案卡状态徽标 + ChatWindow `ontology.unavailable`（§7） |
| 8 | 保存新版本 → 方案页明确提示需重载 | 资产栏保存提示 + 方案卡"需重载" + reload（§5.3/5.4） |
| 19（新增） | 侧边栏四栏可见：学习中心为默认页；构建五路径带状态徽标；资产统一列表；运行按引擎分组 | §4 IA 树 + §5 各节；验收细节见 03 v0.9 验收 19/20 |
| REQ-84/85/87 | 方案字段齐备 / 三要素配置 / 解耦与显式重载 | §5.4 引擎分组页 + 向导 + 正交红线（§6） |

---

## 10. 开放问题

1. **"需重载"判定**：方案是否记录装载时本体版本（loaded_versions）以支持精确提示？后端未提供前，重载按钮常显 + 文案引导。
2. **挂载对话查询**：方案详情"挂载对话"Tab 需后端按 profile 反查会话；无接口则本 Tab 延后。
3. ~~**REQ-103 模式 A 后端排期**~~：**已交付（v0.5）**——OntoChat 多轮引导（会话状态机 + 校验回喂 + 预览入库）已实现，流程页点亮为「可用」。
4. **OntoExtend 工程化评估**：待主人体验引导卡后决定是否立项（对话式扩展 = fork + REQ-82 扩展语料的组合，评估增量成本）。
5. **学习中心内容深化**：方法论卡片 P1 尾先交付 2/5 模块还是全 5 模块精简版？建议全 5 模块精简版（每卡 300~500 字），深度版随 P2。
6. **StageDots"被引用"徽标**：资产卡加"被 N 套方案引用"只读徽标（增强正交可见性），F1 顺手实现。

---

## 11. 迭代记录

| 版本 | 日期 | 变更 | 作者 |
| --- | --- | --- | --- |
| v0.1 | 2026-09-21 | 首版：基于主人"两平面正交"判断与 04 v0.8/03 v0.5 口径，完成现状盘点、错位分析、目标 IA（三 Tab）、页面设计、F1~F4 实施步骤与验收对照 | 智能体开发助理 |
| v0.2 | 2026-09-21 | 评审融合（主人拍板，开放问题 1/3 关闭）：①引擎预检登记 P3 待办（last_error 映射兜底）；②可视化 S4 由 ReactFlow 重构落地（862df01），与 WebVOWL 对比择优/双轨并存（D-O9）；§3 映射表与 §5.1 同步 ReactFlow 现状 | 智能体开发助理 |
| v0.3 | 2026-09-22 | **IA 三代演进定稿（D-O11，主人拍板）**：①IA 由三 Tab 升级为**左侧边栏四栏**——学习中心（默认页）/ 本体构建（按路径分二级模块）/ 本体资产（统一管理）/ 本体运行（按运行方式分二级模块）；②构建五路径分层：**自定义构建承载现有页面（保留 S1~S4，S5~S7 移交资产/运行栏）**+ OntoChat 流程（REQ-103 模式 A 载体，部分可用）+ semantica 流程（入口卡跳转，D-O10 零侵入不破）+ OntoExtend 流程（引导先行，工程化需求池）+ Open Ontologies 流程（引导+回流）；③学习中心 v1 与 REQ-90/91 内容包**提前至 P1 尾**（D-O11）；④REQ-75/76 重定位：pipeline_profile 改路径级工具配置（P2）、打卡机制归学习中心；⑤F1~F5 实施步骤重排，验收对照补 19/20；依据版本同步 03 v0.9 / 04 v0.12 | 智能体开发助理 |
| v0.4 | 2026-09-22 | **F1~F5 实施回写（commit afe82e6，tsc/vite 通过）**：①`web/src/pages/OntologyPage.tsx` 降为薄壳，新模块 `web/src/pages/ontology/`：OntologyModule（四栏侧栏，localStorage `eino.onto.sidebar` + `onto-sidebar-change` 事件路由，page 状态机不动）、BuildPage（五路径，自定义构建保留 S1~S4，`eino.onto.buildPath`）、AssetsPage（列表 + 详情六 Tab：Spec/校验/版本与源码/产物/可视化/TTL 导出）、RuntimePage（引擎分组 + 方案 CRUD/启停/重载 + 详情 Drawer：日志/facade 4 工具/Yasgui/翻译透视）、LearnPage（默认页：七阶段进度 + 任务卡 8 项打卡 `eino.onto.checklist.tasks` + 构建/运行对照 + 示例本体库）；②共享组件迁 `components/`（SpecGraph/SourceView/SparqlConsole/TraceTable）；③正交红线落实：BuildPage 零运行 API、RuntimePage 零仓库写、资产卡"被 N 套方案引用"只读徽标（开放问题 6 关闭）；④`seeds/learning/` 交付：methodology 5 篇 + examples 2 篇 + tasks/tasks.json（开放问题 5 关闭：全 5 模块精简版）；⑤遗留：OntoChat 流程页排期态（开放问题 3 维持）、"需重载"精确判定待 loaded_versions（开放问题 1 维持） | 智能体开发助理 |
| v0.5 | 2026-09-22 | **REQ-103 模式 A 交付（OntoChat 流程点亮，开放问题 3 关闭）**：①后端（ontology-service）：`migrations/003_ontochat.sql` 会话表 + `internal/ontochat/`（Store：SQLite 会话留痕/状态机持久化；Engine：cq→domain→draft/refine 状态机，复用 llmcreate.Creator 平台代理，草稿校验循环内聚 spec.Validate）+ `rest_ontochat.go` 六端点（`/api/ontochat/sessions` CRUD + `turn` + `save`；save 为 REQ-82 预览确认门控）；`llmcreate.Creator` 增 `RawChat`（补全轮归纳用自由文本对话）；②前端：`OntoChatFlow.tsx` 完整对话页（会话列表/切换/删除、消息流、阶段徽标、草稿预览+入库、refine 修正轮），替换 BuildPage 排期态占位，路径徽标升「可用」；client.ts/types.ts 增 ontochat API；③语义保持：模型能力归主平台（`/api/ontology-llm/generate`）、校验归构建平面、草稿必须经用户预览确认入库（REQ-82）；正交不破（构建平面内部扩展，运行平面零改动）；④验证：ontology-service `go build`/`go vet` 通过 + 冒烟（会话 CRUD、cq→domain 状态机、LLM 不可达干净报错）；web `tsc -b`/`vite build` 通过 | 智能体开发助理 |
| v0.6 | 2026-09-22 | **O6 Fuseki 运行时交付（推理对照基座就绪，REQ-94 P1 通用版前置）**：①`runtime-manager/internal/engine/fuseki/`：Apache Jena Fuseki 6.2.0 适配（`FUSEKI_BIN`/`FUSEKI_JAVA` 环境注册，未配置时引擎不注册并留日志）；装载走 `java -cp fuseki-server.jar tdb2.tdbloader`（6.x 精简版无独立脚本）离线灌入**默认图**；服务 `fuseki-server --config config.ttl --port N`（Setpgid 进程组，SIGTERM→8s→SIGKILL）；`reasoning` 配置项（profile config JSON）经 `engine.ReasoningRuntime` 接口透传；②推理实现：`ja:InfModel` 包 `tdb:GraphTDB`（裸默认图）+ `ja:GenericRuleReasoner`（OWL_FB 规则集，启动时从 fuseki-server.jar 解出 `etc/owl-fb.rules` 为绝对 file: URL——Jena 6.x 已移除 `urn:x-jena:` 规则 URL handler）；③**关键坑位（字节码级验证）**：TDB2 的 `tdb:unionDefaultGraph` 仅在数据集级 assembler 生效，图级 `tdb:GraphTDB` 静默忽略——推理侧 baseModel 必须直指裸默认图，数据必须灌默认图（union 视图不含裸默认图，no-inf 侧开 union 反而查空）；④前端 RuntimePage：引擎卡 fuseki「可用」+ 推理徽标，方案向导 fuseki 步骤增推理开关（文案提示对照实验建议同本体建两套一开一关）；⑤验证：四项对照查询全通过——全部三元组 17 vs 694（推理物化 RDFS/OWL axiom）、显式 Module 实例 1 vs 1（显式数据两侧一致）、推断 rdfs:Class 0 vs 60、推断 owl:Class 2 vs 33；**语义级 subClassOf 推断验证**：注入 `CriticalDefect ⊑ Defect` + `D9 rdf:type CriticalDefect` 后，inf 侧查 `?s rdf:type Defect` 返回 D1+D9，no-inf 侧仅 D1；⑥已知限制：TDB2 文件锁无禁用机制（tdb.lock 持有期间 tdbloader 拒载），runtimed 重启后需先清孤儿 Fuseki 进程（Setpgid 使子进程脱离父进程组存活）；方案列表为内存态，runtimed 重启后需重建 | 智能体开发助理 |
| v0.7 | 2026-09-23 | **O11③ 收尾 + P2 启动（REQ-95 版本 diff 前端交付）**：①O11③（commit 201ca3e）：AgentSidePanel 增「MCP Servers」编辑区（Form.List 增删行 + semantica 预设一键挂载 `http://127.0.0.1:8093/mcp`，已挂载自动禁用），保存透传 agent 级 mcp_servers；装配链冒烟验证——mock Streamable HTTP MCP server 挂载时 run.started 无告警（工具拉取成功），停服后出现"连接失败降级"告警不阻断运行（§6.11 降级语义实测）；文档 15 回写 v0.2；**P1 收尾推进序（O11③→O6→O12→P2）仅余 O12**；②REQ-95（commit 9c5b030，P2 首项）：VersionDiff 组件挂入 SourceView 版本 Tab 第三页签——双版本选择（可交换）+ 汇总统计（+/−/~）+ 三集合变更表（字段级 from→to）+ 引用影响表；后端 diffVersions 已有直接复用；冒烟 v1→v2 全 added、v2→v3 definition changed + impact referenced_by=2 均正确 | 智能体开发助理 |
| v0.8 | 2026-09-23 | **O12 学习中心 v1 交付（P1 收尾推进序全清）**：①LearnPage 构建方式对照五路径卡→**六路径卡**（D-O14：增「由知识库构建」引导卡——KB chunk→LLM / KG→直转 / 混合三策略预告 + 与 semantica 流程的对偶关系，工程化随 O13）；②状态徽标刷新：OntoChat 卡 gold 部分可用→green 可用（REQ-103 模式 A 已交付）、Fuseki 卡 gold 随 O6 交付→green 可用（推理对照已激活，卡面更新为"同本体两套方案一开一关 + subClassOf 类型传导"实验指引）、运行对照顶部 Alert info→success；③示例库 Alert 4→5 示例规划（第 5 个"设备故障知识库→设备故障本体"随 O13）；④BuildPage 构建栏同步增第六路径卡（kb 引导态 + KbBuildGuide 引导卡：三策略说明 + 入口端点预告 + 依赖说明）；⑤方法论五模块（REQ-90）与任务卡八项（REQ-91）v0.4 已交付本轮复核无缺口；⑥验证：tsc/vite 通过。**P1 至此全部收尾，后续按 P2 序推进（REQ-96 P2b → REQ-103 模式 B → 90/91 补齐 → 75/76）** | 智能体开发助理 |
| v0.9 | 2026-09-23 | **REQ-96 主线数据灌装全量交付（P2a 同名映射 + P2b 映射向导，commit 561e972）**：①后端（ontology-service）：ingestConfig 增 `type_rules`（列→int/number/date/bool 转换；date 归一 2006-01-02 支持三格式，非法值降级原字符串 + warning 不阻断）与 `multi_value_sep`（关系列一格多目标拆分，空 = 保持 P2a 单值语义）；映射配置存取端点 `GET/PUT /api/ontologies/{id}/ingest-mapping`（artifact `ingest_mapping_json`：不 bump version、ListArtifacts/ListArtifactContents SQL 过滤——不进产物表、不随 fork 复制）；②前端：`CsvIngestPane` 组件挂入资产栏第六页签「CSV 灌装」——上传（前端同口径 CSV 解析出表头）→ 概念/主键/跳行 → 列绑定（属性/关系多选 + 每列类型下拉 + 多值分隔符）→ 预览（统计三格 + warnings 列表 + 前 20 条草稿表）→ 校验入库新版本；映射配置保存/自动带出（404 静默）；client.ts/types.ts 增 ingest-mapping API；③冒烟（onto_defect_mgmt）：date 三格式归一（2026/10/01、20261101→ISO）、bool 归一（no→false/yes→true）、number 12.5 落库为数值、非法 "abc" 降级 + warning；多值拆分一格两条断言；校验门控拦下未定义关系/实例（validation_errors 结构化返回）；apply 成功 version 1→2 且实例入库；skip_rows 跳注释行 + 重复/空主键 warnings；映射 PUT→GET 复取一致、产物列表无 ingest_mapping_json；测试数据已恢复（spec 还原 7 实例、mapping 清除）；④过程中修复：convertCell 失败分支未回填原字符串（降级变 null）——补 `cv = v`；⑤验证：go build/vet、tsc/vite 通过 | 智能体开发助理 |
| v0.10 | 2026-09-23 | **P2 序收官两连发**：①**REQ-90/91 P2 补齐（b20a070）**——方法论五卡各追加深度版小节（CQ 四级分级/三条来源路径差异/三种挂接强度/name-label-URI 三层模型/公理代价与适用边界），seeds 与 LearnPage 卡片同步（卡片增「深度版」折叠项）；新增 failure/orgs 建模说明 README（背景/CQ/决策记录/局限，REQ-90 ② 4 示例全覆盖）；示例库 Alert 更新为 4 示例已齐；②**REQ-75/76 工具链配置页与引导执行（c55730b）**——migrations/004_pipeline.sql（pipeline_profile 表）；internal/pipeline 包（tools.json 七阶段 24 工具候选清单，数据驱动 REQ-77 + 默认工具链模板 + 引导清单生成 + Store CRUD）；REST 八端点（/api/pipelines catalog/list/create/get/put/clone/delete/check）；前端 PipelinePane 挂学习中心第四 Tab「工具链配置」——配置切换条 + 七阶段候选卡片（builtin/guided/managed 徽标，选中即保存）+ guided 打卡清单（`tool:<stage>:<tool_id>` key 空间，与 task: 分列）+ 复制/重置默认/删除；冒烟全通过（catalog 分组/创建默认模板/换 S3=OOPS 生成 checklist/打卡 done_at 落库/clone checklist 清零/删除）；**04 §7 P2 序（REQ-95 → 96 → 103 模式 A → 90/91 补齐 → 75/76）至此全部完成** | 智能体开发助理 |
| v0.11 | 2026-09-23 | **M14 知识库双子模块前端交付（D-KB4 ⑤⑦，commit c3cbe57）**：①KnowledgePage 左栏 RAG/GraphRAG **双子页签**（计数 + 切换选中该组第一个库）+ 库条目与详情头 mode 徽标（rag=blue/graphrag=purple，老数据缺省 = rag）；②新建库弹窗 mode 单选——graphrag 选中时展示 **KG/embedding 两套独立模型一致性提示**（切分质量影响抽取输入 + worker 依赖 + 降级语义，⑦）；③文档表状态列增「KG ✓（tooltip：method/实体/关系数）/KG 降级」徽标（`doc.graphrag` 驱动）；④检索试运行卡片：graphrag 库增「GraphRAG 直查」按钮（`POST /api/kb/{id}/graphrag-search`）+ mode Tag 回显 + degraded=true 时黄色 Alert（worker 不可达已回退向量检索）；⑤client.ts/types.ts：graphragSearchKB + KBSearchResult（mode/degraded/error）+ KnowledgeBase.mode + KBDoc.graphrag；tsc/vite 通过 | 智能体开发助理 |

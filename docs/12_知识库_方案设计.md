# 知识库方案设计（智能体平台）

> 状态：Draft v0.4
> 日期：2026-09-23
> 文档性质：**活文档**。知识库模块的唯一设计事实源；技术栈归属与可插拔抽象锚点仍在《02_智能体_技术方案设计.md》（v0.11 起 §6.9 收编为对接契约；v0.25 增双子模块接口）。
> 关联：11《知识库_需求文档.md》（v0.2，需求侧同步 REQ-107）、01 v0.10（对话侧引用契约；原 v0.12，已重编号）、02 v0.25（平台架构/技术栈/SSE 协议）、04 v0.12+（本体侧 semantica worker :8093 接口契约）
> 会话分工：本会话维护；与本体文档（03/04）同模式独立演进。

---

## 1. 概述与演进

| 项 | 内容 |
| --- | --- |
| 模块定位 | 主平台内置**知识库模块（v0.2 双子模块）**：文档入库 → 切分 → Embedding → chunk 池；**子模块 A「向量检索 RAG」**走 VectorStore 路径（chunk→向量→TopK）；**子模块 B「GraphRAG 检索」**走 semantica worker 路径（chunk→KG→向量+图混合问答）。两个子模块**共享 chunk 池**，由 `kb.mode` 字段区分，通过左侧边栏**顶部 tab 切换**（同一模块页内）。 |
| 设计基调 | **开源优先、组件级集成**（01 Q-18）：用开源组件拼装最小闭环，自研仅保留学习对照 fallback；**不引入平台级成品作为知识库本体**（结论与依据见 §5）。**GraphRAG 不自研**，复用本体侧 semantica worker :8093（钉版本 v0.6.8+），主平台仅做"消费入口 + KB 标记 + 检索转发"，KG 实体/关系落 semantica 侧，不在本平台重存。 |
| 演进脉络 | 02 v0.3：SQLite 简单 RAG（自研余弦 TopK）→ 02 v0.6：开源化重写（eino-ext 适配优先 + Qdrant 首选，SQLiteRetriever 降级 fallback）→ **12 v0.1：自 02 §6.9 拆出独立成档**，并完成开源可选方案对比分析（含微信 AI WeKnora 0.8.0）→ **本 v0.2：拆为 RAG / GraphRAG 两个子模块**（D-KB4，2026-09-23 主人拍板）；反转 D-KB2 中 GraphRAG 进阶池条目（升级为 P1 内置子模块）；复用本体侧 semantica worker 0 自研；接口扩展 `kb.mode` + `/api/kbs/{id}/graphrag-search`。 |

## 2. 需求承接

需求事实源见 11：REQ-07/50/51/53/54/56/57（P1，RAG 子模块原 11 v0.1 全部保留）+ REQ-107（v0.2 新增：知识库模块 IA 拆分为「向量检索 RAG」「GraphRAG 检索」两个子模块，左侧边栏 tab 区分）+ KB-1（选型确认）/KB-2（进阶池）/KB-3（独立服务备选，P2）。本文档只维护设计。

## 3. 总体设计

### 3.1 管道视图（v0.2 双轨：RAG 子模块 + GraphRAG 子模块）

**子模块 A「向量检索 RAG」**（`kb.mode='rag'`，v0.1 现状原样保留）：

```
索引管道（异步，状态机）                    检索管道（对话内同步）
┌────────────┐   ┌──────────┐   ┌─────────────────┐
│ 文档入库    │→ │ 切分器    │→│ Embedding(模型连接)│
│ txt/md/粘贴 │   │ 块/重叠   │  │ OpenAI 兼容       │
└────────────┘   └────┬─────┘   └────────┬────────┘
                      ↓                   ↓
              chunk 表(正文+状态)     VectorStore 接口(可插拔)
                      ↓            ┌────┴─────┐
                索引状态回写        │ Qdrant(P1)│  │ SQLite fallback│
              (待处理/处理中/就绪/失败) └──────────┘  └───────┘

对话提问(选了知识库+RAG) → retrieve(query, topK, threshold) → 片段+得分
   → 拼接上下文 → SSE `retrieval` 事件(来源+摘要，REQ-17) → Agent 生成
```

**子模块 B「GraphRAG 检索」**（`kb.mode='graphrag'`，v0.2 新增，复用本体侧 semantica worker）：

```
索引管道（异步，状态机）                检索管道（对话内同步）
┌────────────┐   ┌──────────┐   ┌──────────────────┐
│ 文档入库    │→ │ 切分器    │→│ Embedding(模型连接)│
│ txt/md/粘贴 │   │ 块/重叠   │  │ OpenAI 兼容       │
└────────────┘   └────┬─────┘   └────────┬─────────┘
                      ↓                   ↓
              chunk 表(正文+状态)    ─→ 调用 semantica worker
                   (chunk 池共享)         :8093 POST /graphrag/ingest
                                          (chunk → KG 抽取 → 实体/关系)
                                              ↓
                                       semantica 侧 graph_store
                                       (主平台不重存)

对话提问(选了知识库+GraphRAG) → POST /api/kbs/{id}/graphrag-search
   → 反代 → semantica :8093 /graphrag/query
   → SSE `retrieval` 事件(类型=graphrag，含 entities/relations/claims) → Agent 生成
```

**两条管道共享 chunk 池**：文档入库一次，chunk 同时写入 `chunk` 表 + 触发 GraphRAG 子模块的 semantica 抽取（若该 KB 启用 graphrag 模式）；切换检索模式不需要重新入库。

### 3.2 可插拔 Store 抽象（自 02 §6.9 原样承接，接口不变）

```go
type VectorStore interface { // 两种实现
    Upsert(ctx, kbID string, chunks []Chunk) error
    Search(ctx, kbID string, vec []float32, topK int, minScore float64) ([]Hit, error)
    DeleteByDoc(ctx, kbID, docID string) error
}
// QdrantStore（P1 首选）：eino-ext 现成 indexer/retriever 适配优先（M6 首日核对可用组件清单后定稿对接方式），
//   无适配则直连 Qdrant REST（collection=kb_{id}，cosine，point id=chunk id）
// SQLiteStore（fallback）：沿用 v0.5 SQLiteRetriever（余弦 TopK），保留为无外部依赖对照（不删）
```

- 选择逻辑：配置 `KB_STORE=qdrant|sqlite`；Qdrant 启动探测不可达 → 自动落 SQLite fallback 并在索引状态提示（降级语义，不阻断建库；探测为部署层逻辑，不属接口成员）。
- **不编造组件**（02 §6.9 既有约定，转为 KB-O1）：eino-ext 具体 indexer/retriever 适配是否齐备在 M6 实现首日核对官方仓库后定稿；均不适用则 Qdrant 直连 REST，仍属开源优先范畴。

### 3.3 数据模型（自 02 移入；v0.2 增 `kb.mode` 字段）

三表沿用 02 v0.5 结构（`kb` / `doc` / `chunk` + 状态字段）；`knowledge_chunk` 增列 `store_backend TEXT DEFAULT 'sqlite'`、`vector_ref TEXT`；**v0.2 增 `kb.mode` 区分检索路径**：

| 表 | 关键列 | 说明 |
| --- | --- | --- |
| `kb` | id, name, brief, top_k, threshold, **`mode` TEXT DEFAULT 'rag'` ('rag'`/`'graphrag')`, created_at | 库级检索参数（REQ-57）+ 检索模式（v0.2 新增 D-KB4）；同一库只有一种 mode，切换 mode = 重建 KG 或换库 |
| `doc` | id, kb_id, name, status, chunk_count, dim, updated_at | 文档单位（REQ-51/54）；status：待处理/处理中/就绪/失败 |
| `chunk` | id, doc_id, kb_id, content, status, store_backend, vector_ref(BLOB 或 Qdrant point id) | 正文恒存 SQLite；Qdrant 路径向量存 Qdrant，fallback 路径 BLOB 直存；**两子模块共用 chunk 池**（rag 直接用 vector_ref，graphrag 把 chunk 文本喂 semantica 抽 KG） |

- 对话关联：`conversation.kb_id`（01 REQ-19a）留 02 平台库；知识库侧不感知会话。
- **KG 落点（v0.2 决定）**：GraphRAG 子模块的实体/关系/事件**只落 semantica worker 侧 graph_store**，不在主平台 SQLite 重存；主平台在检索时通过 `/api/semantica/*` 反代调 worker 查询接口（04 §4.9 已有的反代机制）。好处：避免双库一致性负担 + 让 KG 与本体侧 semantica 共享审计/版本管理能力。

### 3.4 API 与事件契约（v0.2 双子模块）

| 契约 | 内容 | 兼容性 |
| --- | --- | --- |
| REST | `/api/kbs` CRUD（含 `mode` 字段过滤）；`/api/kbs/{id}/docs`（POST 粘贴文本/上传 txt md，DELETE 级联）；`POST /api/kb/{id}/search-preview` 试运行直接返回命中（02 §6.9 原路径承接，REQ-57） | 自 02 §5 承接，不变 |
| REST（v0.2 新增） | `POST /api/kbs/{id}/graphrag-search`（body: `{query, top_k?, min_score?}` → `{hits: [{content, entities, relations, claims, score}]}`）—— RAG 子模块 KB 调该端点返回 400；GraphRAG 子模块 KB 走 `/api/semantica/*` 反代 → semantica :8093 `/graphrag/query` | 新增，配套 D-KB4 |
| SSE | `retrieval` 事件：`{kb_id, doc_name, chunk_preview, score, **mode**}`（mode ∈ {'rag','graphrag'}，v0.2 新增字段），回答前发出（REQ-17）；上下文注入格式 `[片段 doc:seq score]`（02 §6.9 原口径，rag 子模块）/ `[KG 实体/关系 doc:seq score]`（graphrag 子模块，含 entity_type/relation_type 标注） | 兼容（mode 字段为可选，老客户端忽略） |
| 部署 | Qdrant :6333 可选容器（02 §4.1 已列入）；SQLite 路径零新增依赖；semantica worker :8093 由本体侧 O11 引入（04 §4.9 已落地），知识库侧零自研 | 不变（GraphRAG 子模块不引入新进程） |

### 3.5 KB 作为本体构建数据源（v0.3 新增，D-O14 配套）

> **背景**：本体模块构建栏第六路径「由知识库构建本体」（D-O14，2026-09-23 主人拍板方案 A）的数据源全部来自 KB——RAG / GraphRAG 两个子模块的 KB 均可作为本路径输入。详见 03 §2.10 REQ-108 + 04 §3.7。

**3.5.2 哪些 KB 可作为本体构建数据源**

| KB 模式 | 适用抽取策略 | 路径入口 | 依赖 | 状态 |
| --- | --- | --- | --- | --- |
| `kb.mode='rag'` | **策略 A**：KB chunk 池 → LLM 抽 spec_json（复用 `/api/ontology-llm/generate` REQ-82 + REQ-90 CQ 引导） | `POST /api/ontologies/build-from-kb` body `{kb_id, strategy:'chunk-llm', cq_hints?}` | 仅需 REQ-82 已就绪（已就绪）+ REQ-90 CQ 引导（已就绪） | **部分可用**（最早随 M6 KB 完成 + M14 KB 双子模块 + REQ-82 已就绪即可立项） |
| `kb.mode='graphrag'` | **策略 B**：KB GraphRAG KG → 直转 spec_json（v0.13 semantica worker `/graphrag/ingest` 已扩展接受 chunk 集合，主平台薄映射层 <300 行 entity→Concept / relation→Relation / claim→Attribute）；**策略 C**：KG 作初稿 + LLM 校验 + 补全 | `POST /api/ontologies/kg-to-spec-json` body `{kb_id, strategy:'kg-direct'|'kg-llm-hybrid'}` | 策略 B 依赖 v0.13 semantica worker chunk 接受（已就绪）；策略 C 额外依赖 REQ-82 | **P2 后评估**（KG 直转保真度 + LLM 抽取粒度失控两条风险） |

**3.5.3 KB 选择器契约**

新增 REST 端点：`GET /api/kbs/selectable-for-ontology-build`（无 body）→ 返回 `{kbs: [{id, name, mode, chunk_count?, entity_count?, relation_count?, created_at}]}`——列出所有可作为本体构建数据源的 KB，**RAG 子模块 KB 含 chunk_count**，**GraphRAG 子模块 KB 含 chunk_count + entity_count + relation_count**；前端 KB 选择器按 mode 徽标分组（KB 卡片左上角徽标「RAG / GraphRAG」），点击进入抽取策略选择页。

**3.5.4 数据流闭环**

```
用户操作：本体构建栏第 6 条「由知识库构建本体」
  ↓
GET /api/kbs/selectable-for-ontology-build → KB 选择器
  ↓
用户选 KB + 策略（A/B/C）+ CQ 引导（REQ-90 三档：自动生成 3~5 个能力问题 / 用户自定义 / 跳过）
  ↓
POST /api/ontologies/build-from-kb  (策略 A) | /api/ontologies/kg-to-spec-json  (策略 B/C)
  ↓
策略 A → /api/ontology-llm/generate  (复用 REQ-82)
策略 B → 薄映射层 <300 行 entity→Concept / relation→Relation / claim→Attribute
策略 C → 策略 B + LLM 校验 + 补全（再调 REQ-82）
  ↓
spec_json 草稿（必须经 S3 校验循环，最多 3 轮；03 §2.10 REQ-108 验收）
  ↓
用户确认 → 入库
```

**3.5.5 与现有 KB 端点的关系**

| 端点 | 用途 | 是否本路径相关 |
| --- | --- | --- |
| `POST /api/kbs` | 创建 KB（v0.2 增 mode 字段） | 不直接相关；本路径只读 |
| `GET /api/kbs` | 列 KB | 不直接相关；本路径用 `/api/kbs/selectable-for-ontology-build` 子集 |
| `POST /api/kbs/{id}/graphrag-search` | GraphRAG 检索（KB 内） | 不直接相关；本路径走 spec_json 生成 |
| `POST /api/kbs/{id}/chunks` | 列 KB 内 chunk 集合（v0.2 引入，本路径隐含依赖） | 间接依赖（策略 A 喂入 LLM） |
| **`GET /api/kbs/selectable-for-ontology-build`（v0.3 新增）** | 本路径专用 KB 列表 | **本路径独有** |

**3.5.6 学习价值闭环**

D-KB4 v0.25 把 KB 拆为 RAG / GraphRAG 双子模块；D-O14 v0.26 让 KB 反哺本体构建——「文本→三种语义资产形态」完整矩阵成形：
- **文本 → RAG 片段**（检索用，不持久化为本体资产）
- **文本 → KG**（semantica 消费，作为 GraphRAG 子模块检索端点数据）
- **文本 → 本体**（D-O14，KB 作为本体构建数据源，本路径三种抽取策略）

——与本体侧四环节（构建/运行/消费/审计）形成「文本→不同形态资产」完整映射，教学价值闭环。

## 4. 学习价值设计（LG-11 对齐）

| 环节 | 开源组件/机制 | 学习点 |
| --- | --- | --- |
| 切分 | 自研薄层（块/重叠参数化） | chunking 对召回质量的影响（REQ-57 试运行可对照） |
| Embedding | 模型连接（OpenAI 兼容） | embedding 模型差异（BGE/OpenAI 系列）对召回的影响 |
| 向量检索 | Qdrant（cosine + HNSW）/ SQLite 余弦全扫 | 向量库索引 vs 暴力扫描的性能差异；两 Store 实现同一接口 |
| 上下文注入 | Eino Retriever 抽象 + Prompt 拼接 | RAG 在编排框架中的位置（对比 02 §5 runs 流程） |
| fallback 思想 | SQLiteStore | 降级设计与无依赖部署权衡（与 WeKnora Lite 同构，见 §5.3） |

## 5. 开源可选方案分析（本 v0.1 核心新增）

### 5.1 证据台账

| ID | 事实 | 来源 | 新鲜度 | 置信度 |
| --- | --- | --- | --- | --- |
| E1 | WeKnora 0.8.0（2026-09-16 前后发布）：腾讯微信 AI 团队开源知识库问答/RAG+Agent 平台；MIT 许可；GitHub 快照约 24.4k stars / 3.4k forks；主项目 Go 后端 + Vue 3 前端 + Python docreader（gRPC 文档解析）+ 桌面端（Wails）+ MCP Server + 微信小程序 | [WeKnora 官方文档-产品介绍](https://weknora.weixin.qq.com/docs/01-getting-started/01-introduction)、[电子工程专辑评测](https://www.eet-china.com/mp/a525707.html) | 2026-09-16/17 | high |
| E2 | WeKnora 能力面：anydoc 多格式解析（PDF/Word/PPT/Excel/MD/HTML/图片/音频）、OCR/表格抽取/VLM 图片描述/ASR 转写、父子分块与自适应分块、向量+BM25 混合检索（RRF 融合）、Rerank、查询改写、意图识别、FAQ 索引、Wiki 生成、GraphRAG（Neo4j）、ReAct Agent + Skill 沙箱（Docker/E2B/Cube）、长期记忆、IM 渠道接入（企微/飞书/Slack/Telegram 等） | WeKnora 官方文档、[OSChina 报道](https://www.oschina.net/news/502612) | 2026-09-16/20 | high |
| E3 | WeKnora 部署形态：Docker Compose（profile 可选 neo4j/minio/langfuse/full）、Helm；**Lite 单二进制模式**——单进程 + SQLite（FTS5 全文）+ sqlite-vec 向量 + 内存队列，零外部依赖；另有桌面应用/Homebrew | [WeKnora 官方文档-安装部署](https://weknora.weixin.qq.com/docs/01-getting-started/02-installation) | 2026-09-16 | high |
| E4 | WeKnora 向量库可插拔：pgvector、Elasticsearch、Milvus、Weaviate、Qdrant、Apache Doris、腾讯 VectorDB（含多库 fanout，v0.6.0 起）；标准部署含 PostgreSQL/pgvector + Qdrant + 可选 Neo4j 微服务集群 | [架构分析](https://martianlee.github.io/kb/2026-05-30-weknora-architecture)、[部署实战](https://blog.csdn.net/a15835774652/article/details/162071413) | 2026-05/06 | high |
| E5 | RAGFlow 0.27.0（2026-08-24）：Knowledge Compilation Engine（Wiki/Graph/Tree/MindMap 等知识工件，替代旧 GraphRAG/RAPTOR）+ Agentic Retrieval 四档思考模式；Python 后端（Go 侧迁移推进中，v0.26 起重构）；定位"智能体数据底座"；自研 Infinity 向量库；社区评为功能强但运维复杂（多解析器/多模型商/编译管道） | [RAGFlow 官方博客 0.27](https://ragflow.io/blog/ragflow-0.27-knowledge-compilation-and-agentic-retrieval)、[gittimes 评述](https://gittimes.com/editions/2026-08-27/ragflow-v0-27-0-adds-knowledge-compilation-and-agentic-thinking-modes/) | 2026-08-24/27 | high |
| E6 | Dify：LLMOps 平台（工作流/Agent/知识库一体的应用平台），知识库为其子功能；本项目此前未评估其作为知识库组件引入的可行性（非 Go 栈、平台级） | 公开常识 + 平台定位 | — | medium |
| E7 | 本项目语境（内部事实）：学习目标 LG-11（学 RAG 闭环搭建而非用成品）；Non-Goals 明确排除 PDF/OCR/Rerank/混合检索等进阶能力；单机 SQLite 部署基线；前端已拍板全蚂蚁生态 React（01/02/06）；知识库为平台内一个 P1 模块而非独立产品 | 01 v0.10（原 v0.12）/ 02 v0.11 / 06 v0.7 | 2026-09-20/21 | high |

### 5.2 候选盘点

| 层级 | 候选 | 形态 | 一句话定位 |
| --- | --- | --- | --- |
| 平台级成品 | **WeKnora 0.8.0**（E1/E2/E3/E4） | 独立全栈系统（Go+Vue+Python，自带会话/Agent/编排） | 企业知识库 RAG+Agent 一体平台，功能面目前最全且活跃 |
| 平台级成品 | RAGFlow 0.27（E5） | 独立全栈系统（Python 为主） | 深文档理解 + 知识编译，企业级但运维重 |
| 平台级成品 | Dify（E6） | 独立应用平台 | LLM 应用平台，知识库为附属能力 |
| 组件级 | **eino-ext 适配 + Qdrant**（现状方案） | Go 组件库 + 单容器向量库 | 与 Eino 编排同栈，随用随接 |
| 组件级 | sqlite-vec / SQLite FTS5 | 进程内库 | 零依赖轻量检索（WeKnora Lite 同款思路，E3） |
| 组件级 | LangChain/LlamaIndex 类框架 | Python/JS 框架 | 与本项目 Go 栈不符，仅作参照 |

### 5.3 WeKnora 0.8.0 深度评估（主人点名对象）

**能力对比（WeKnora 0.8.0 vs 本项目知识库当前范围）：**

| 维度 | WeKnora 0.8.0 | 本项目知识库（11 需求） | 差距性质 |
| --- | --- | --- | --- |
| 文档接入 | 20+ 格式 + OCR/表格/VLM/ASR + 多平台同步 | 粘贴文本 + txt/md | **WeKnora 强**，但超出本项目 Non-Goals（11 KB-2 进阶池） |
| 检索 | 混合检索 + RRF + Rerank + 改写 + 意图识别 | 向量 TopK + 阈值 | WeKnora 强，同属进阶池 |
| 知识组织 | Wiki / GraphRAG / FAQ / 预生成问题 | 无 | WeKnora 强，进阶池 |
| Agent 能力 | 自带 ReAct Agent + Skill 沙箱 + 长期记忆 | 不适用（本项目 Agent 编排属主平台核心自研区，Eino ADK） | **职责重叠**：引入即第二套 Agent 体系 |
| 集成形态 | 完整成品：整体部署经 API/MCP 使用（自带 MCP Server） | 平台内模块：进程内组件 + 共享库表 | 形态不同层 |
| 技术栈 | Go 后端 ✓ / **Vue 3 前端**（与已拍板蚂蚁生态 React 冲突）✗ / Python 解析服务（多一个运行时）✗ | Go + React（蚂蚁生态） | 部分冲突 |
| 资源 | 标准：PostgreSQL+pgvector+Qdrant+（可选 Neo4j）多容器；**Lite：单二进制 SQLite+FTS5+sqlite-vec 零依赖** | SQLite 单机 + Qdrant 可选容器 | 相当（Lite 模式与本项目 fallback 思路同构） |
| 许可与活跃 | MIT、~24.4k stars、微信 AI 团队维护（E1） | Qdrant Apache-2.0；eino-ext 云原生社区 | 均可商用 |

**逐条回答"是否有比 WeKnora 0.8.0 更合适的方案"：**

1. **若问题是"知识库功能谁更强"**：WeKnora 0.8.0 在开源 RAG 平台里属第一梯队（功能面全、MIT、微信 AI 背书、活跃），RAGFlow 0.27 的深文档理解/知识编译与之互有胜负，Dify 偏应用平台。**没有明显更强的成品平台**。
2. **若问题是"对 eino-multiagent-lab 哪个更合适"**（真实语境，E7）：**维持现状方案（eino-ext + Qdrant + SQLite fallback）更合适**，WeKnora 不是替代项：
   - **学习目标冲突**（LG-11）：本项目知识库的存在意义是学 RAG 闭环搭建；引入成品即放弃该学习目标——相当于用现成答案替代练习题。
   - **架构职责重叠**：WeKnora 自带会话、Agent、编排、沙箱——与本平台核心（Eino ADK 多智能体编排）整体重复；引入等于"平台里再装一个平台"。
   - **集成成本不对称**：其价值集中在文档解析与进阶检索（OCR/GraphRAG/混合检索），全部在 11 KB-2 需求池（本项目明确不做）；为池内需求引入整套外部系统不划算。
   - **技术栈摩擦**：Vue 3 前端无法融入已拍板的 React/蚂蚁生态界面体系（06 v0.7）；Python docreader 增加运行时；标准部署多容器超单机学习尺度（Lite 模式虽轻但那样只用它的 SQLite 检索，价值更低）。
   - **集成口径**：WeKnora 是"另一个 OpenOntologies 式外部服务"的正确候选——经其 MCP Server 独立接入、Agent 挂载工具，与平台内组件级知识库**并行不替代**（11 KB-3，P2 备选）。
3. **组件层是否换血**：不换。Qdrant（Apache-2.0、单容器、Eino 生态适配在核对清单 KB-O1）与 WeKnora 内部可插拔向量库选项（E4）重叠，能力满足 TopK 检索；sqlite-vec 可作为 SQLiteStore 的演进选项（当前余弦全扫在学习尺度下够用，不引入 C 扩展依赖）。

### 5.4 选型决策（承接 01 Q-18 / KB-1，收敛为正式决策）

| 决策点 | 结论 | 放弃方案 | 理由 | 反转条件 | 置信度 |
| --- | --- | --- | --- | --- | --- |
| D-KB1 知识库实现形态 | **平台内组件级**：eino-ext 适配优先 + Qdrant（P1），SQLiteRetriever fallback（学习对照） | 平台级成品（WeKnora/RAGFlow/Dify 整体引入） | §5.3：学习目标、架构重叠、进阶池边界、技术栈一致性 | 董奎将知识库定位改为独立产品/需要完整企业知识库能力 | high |
| D-KB2 进阶能力获取路径 | OCR/混合检索等进阶能力**不自研不引入**，留 11 KB-2 池；触发时**优先评估 KB-3 独立服务集成**（WeKnora MCP / RAGFlow API，对标 OO 双轨模式）——**GraphRAG 例外**：升级为 P1 内置子模块 D-KB4 | 自研进阶 RAG；替换主方案 | GraphRAG 触发概率高（主人 09-23 拍板）；不重研发，复用本体侧 semantica worker :8093 0 自研 | WeKnora MCP / RAGFlow API / 自研三项中成本评估出现更优 | high（GraphRAG 部分已收紧） |
| D-KB3 向量库 | Qdrant 维持首选（单容器 REST/gRPC，cosine） | pgvector/Milvus/Weaviate | Eino 生态适配路径最短（KB-O1 核对后定稿）；单机学习尺度够用 | eino-ext 适配均不可用且直连成本高 → 评估 pgvector（与主库合并） | high（待 KB-O1 定稿） |
| D-KB4 **知识库模块双子模块拆分（v0.2 新增，2026-09-23 主人拍板）** | 知识库拆为「**向量检索 RAG**」「**GraphRAG 检索**」两个子模块；同一模块页内**左侧边栏 tab 区分**（顶部 tab，KG 实体徽标「RAG / GraphRAG」）；**共享 chunk 池**（一份文档入库同时供两子模块，GraphRAG 子模块 chunk 入库后异步触发 semantica worker 抽 KG），`kb.mode` 字段区分（默认 'rag'）；GraphRAG 子模块复用本体侧 semantica worker :8093（钉版本 v0.6.8+，已实现 ingest_ttl + GraphRAG query；v0.2 扩展 `POST /graphrag/ingest` 接受任意 chunk 集合）；KG 实体/关系**只落 semantica 侧**，主平台不重存；语义检索端点 `POST /api/kbs/{id}/graphrag-search` 经 `/api/semantica/*` 反代；**v0.3 追加**——同时作为本体构建数据源（详见 §3.5）：RAG 模式 KB 走策略 A（chunk→LLM 抽 spec_json）；GraphRAG 模式 KB 走策略 B/C（KG→spec_json 直转 / KG+LLM 混合）；KB 选择器 `GET /api/kbs/selectable-for-ontology-build` 按 mode 徽标分组 | 单 RAG 路径延续；自研 GraphRAG；独立再起一个新知识图谱服务 | ①学习平台需演示「同一文档库的两种检索面」（精确 chunk 检索 vs 语义图谱检索）与本体侧"facade 精确查询 vs semantica 语义问答"形成两层对照（基础 RAG 层 + 本体语义层）；②semantica worker 已在 04 §4.9.2 引入，0 重研发；③子模块切换不破坏 chunk 一致性；**④v0.3 反哺本体构建——KB→本体构建闭环（D-O14）与本体→KG 消费闭环（semantica）形成「文本→三种语义资产形态」完整矩阵**（RAG 片段 / KG / 本体） | 董奎改主意不需 GraphRAG 子模块 / semantica 钉版本更新导致接口不稳 / 子模块 2 教学价值低于预期 / **D-O14 路径 P2 后评估发现 KB 数据源不满足抽取质量** | high |

**结论一句话**：当前方案（eino-ext + Qdrant + SQLite fallback）就是针对本项目语境"比 WeKnora 0.8.0 更合适"的方案——因为合适性由学习目标与架构边界决定，而非功能面；WeKnora 0.8.0 作为 P2 独立集成候选归档（11 KB-3），其 Lite 模式（SQLite+FTS5+sqlite-vec 零依赖）与本项目 fallback 思路同构，反向印证了轻量路线的合理性。

## 6. 里程碑与验证（M6 对应，承接 02 M6；v0.2 增 M6-KB4 GraphRAG 子模块）

| 里程碑 | 交付 | 验收 |
| --- | --- | --- |
| M6-KB1 | Store 接口 + SQLiteStore fallback + 三表 + API | SC-6 前半：建库/导入/切分/检索试运行可用（零外部依赖跑通） |
| M6-KB2 | QdrantStore（eino-ext 适配或直连，KB-O1 定稿）+ 降级切换 | 两 Store 检索结果一致（11 验收第 2 条）；停 Qdrant 自动落 fallback 并提示 |
| M6-KB3 | 对话注入 + `retrieval` 事件（v0.2 增 `mode` 字段） | SC-6 全流程：选库提问，回答引用召回来源（REQ-17/56）；mode 字段默认 'rag' 老客户端忽略 |
| M6-KB4（v0.2 新增） | GraphRAG 子模块：①`kb.mode` 字段 + 创建表单 mode 选择器；②入库异步触发 semantica worker `:8093 POST /graphrag/ingest` 抽 KG；③`POST /api/kbs/{id}/graphrag-search` 端点经 `/api/semantica/*` 反代；④`retrieval` SSE 事件 mode='graphrag' 字段填充；⑤UI tab 区分；⑥semantica worker 不可达 → 检索返回 `ontology.unavailable` 降级卡（本体侧已有语义复用） | SC-7：选 GraphRAG 库提问，回答引用召回的实体/关系/claims（与 RAG 子模块并列对照可演示） |

## 7. 风险登记

| 风险 | 触发 | 影响 | 缓解 | 降级 | Owner |
| --- | --- | --- | --- | --- | --- |
| eino-ext 无可用 Qdrant 适配（KB-O1） | M6 首日核对 | 对接方式变更 | 已预设直连 REST 备选（仍开源优先） | SQLite fallback | 智能体开发会话 |
| embedding 连接误配（DeepSeek 无 embedding，01 REQ-46） | 建库向量化失败 | 索引失败 | REQ-19b 引导提示 + 索引状态失败可重试 | 仅文本不向量（不提供，报错阻断） | 智能体开发会话 |
| Qdrant 容器不可达 | 运行期 | 检索失败 | 启动探测 + 健康检查 | 自动落 SQLiteStore（同接口）并状态提示 | 智能体开发会话 |
| 库级换 embedding 模型导致维度不一致（KB-O3） | 用户换模型 | 旧向量作废 | 文档维度记录于 doc 表，变更时提示重建 | — | 待董奎确认策略 |
| **semantica worker 钉版本接口不稳（KB-O5，v0.2 新增）** | semantica 大版本升级 | `/graphrag/ingest` 或 `/graphrag/query` 字段变化 → 知识库侧 GraphRAG 子模块断链 | 钉版本 v0.6.8+；版本变更经知识库文档评审 | 降级到 RAG 子模块（graphrag KB 临时等同 rag）；本体文档同步 | 本体会话 + 智能体会话双 Owner |
| **GraphRAG 子模块 worker 不可达（v0.2 新增）** | semantica worker 未启动 / 故障 | GraphRAG KB 检索失败 | 启动探测 + UI 状态徽标；对话内 `ontology.unavailable` 卡复用本体侧语义 | RAG 子模块可继续用；同库 chunk 数据保留 | 智能体开发会话 |
| **KG 抽取与 embedding 模型一致性（v0.2 新增）** | 用户换 embedding 模型 | semantica 侧 embeddings 表与 chunk 错位 | doc 维度记录 + 切换时提示重建 KG（DELETE semantica 侧 graph_store + 重 ingest） | 仅重 ingest 失败时回退到 RAG | 智能体开发会话 |

## 8. 开放问题

| 优先级 | 问题 | 归属 |
| --- | --- | --- |
| KB-O1（P1） | eino-ext indexer/retriever 组件清单核对（Qdrant/Milvus/redis 适配）→ M6 首日定稿对接方式 | 智能体开发会话 |
| KB-O2（P2） | WeKnora/RAGFlow 独立服务集成（11 KB-3）是否启动 | 董奎 |
| KB-O3（P2） | embedding 模型变更时库级重建策略（锁定维度 or 提示重建） | 董奎 |
| **KB-O4（v0.2 新增，P1）** | semantica worker `:8093 POST /graphrag/ingest` 接口扩展（接受任意 chunk 数组而非仅 TTL）→ 需本体会话与知识库会话双侧确认字段契约（chunk_id/source/embedding_dim 等） | 本体会话 + 智能体会话 |
| **KB-O5（v0.2 新增，P1）** | semantica worker 钉版本策略——主平台启动时校验 worker 版本 ≥ v0.6.8，否则知识库 GraphRAG 子模块不可用并提示升级 | 本体会话 |
| **KB-O6（v0.2 新增，P2）** | 知识库子模块切换与对话 `enable_kb_retrieval` 配置粒度——同一对话是否允许同时启用 RAG + GraphRAG 双 KB 并 merge 检索（场景：先用 chunk TopK 找候选段落，再用 KG 拓展实体上下文），还是单 KB 单 mode 二选一 | 董奎 |

## 9. 版本记录

> 后续每次变更请追加一行，不要覆盖历史。

| 版本 | 日期 | 变更 | 确认人 |
| --- | --- | --- | --- |
| v0.1 | 2026-09-21 | 首版：知识库设计自 02 v0.11 §6.9 拆出独立成档（管道视图/Store 抽象/三表模型/API 契约原样承接）；新增 §5 开源可选方案分析——WeKnora 0.8.0 深度评估（E1~E5 证据：腾讯微信 AI、MIT、~24.4k stars、Go+Vue3+Python 架构、Lite 单二进制、GraphRAG/Skill 沙箱等）与 RAGFlow 0.27 对照，结论 D-KB1~3：**维持组件级方案，WeKnora 列为 P2 独立集成候选（不替换）**，进阶能力留需求池 | 董奎 |
| v0.2 | 2026-09-23 | **双子模块拆分（D-KB4，主人拍板）**：①§1 模块定位改为「RAG + GraphRAG 双子模块，共享 chunk 池」；②§3.1 管道视图拆为双轨；③§3.3 `kb.mode` 字段区分（默认 'rag'）；④§3.4 增 `POST /api/kbs/{id}/graphrag-search` 端点 + `retrieval` SSE 事件 `mode` 字段；⑤§5.4 增 D-KB4（GraphRAG 复用本体侧 semantica worker :8093，钉版本 v0.6.8+，KG 落 semantica 侧，反转 D-KB2 中 GraphRAG 部分）；⑥§6 里程碑增 M6-KB4（GraphRAG 子模块：mode 字段/异步抽 KG/检索端点/SSE 兼容/UI tab/降级卡复用）；⑦§7 风险增 3 条（钉版本接口不稳/worker 不可达/KG 与 embedding 一致性）；⑧§8 开放问题增 KB-O4（semantica 接受 chunk 数组扩展）/O5（钉版本校验）/O6（双 KB merge 检索） | 董奎 |
| v0.3 | 2026-09-23 | **KB 作为本体构建数据源（D-O14 配套，主人拍板方案 A）**：①§3.5 新增章节（含 3.5.2 KB 模式与适用抽取策略对照表 / 3.5.3 KB 选择器契约 `GET /api/kbs/selectable-for-ontology-build` / 3.5.4 数据流闭环 ASCII 图 / 3.5.5 与现有 KB 端点的关系 / 3.5.6 学习价值闭环——「文本→三种语义资产形态」完整矩阵）；②§5.4 D-KB4 行追加：v0.3 反哺本体构建——RAG 模式 KB 走策略 A、GraphRAG 模式 KB 走策略 B/C，KB 选择器按 mode 徽标分组；理由列增第 ④ 条「KB→本体构建闭环与本体→KG 消费闭环对偶」，触发条件列增「D-O14 路径 P2 后评估发现 KB 数据源不满足抽取质量」 | 董奎 |
| v0.4 | 2026-09-23 | **M14 双子模块工程交付回写（commit c3cbe57，D-KB4 ①~⑧ 全项）**：①`kb.mode` 落库（008 迁移 DEFAULT 'rag'，⑧老数据兼容）+ Create/Update graphrag 归一化 + scanKB 防御默认；②共享 chunk 池语义落地——RAG 与 GraphRAG 共用切分/Embedding/chunks，graphrag 模式索引成功后额外推 worker `/graphrag/ingest`（同步短超时，degraded 不阻断，⑥），结果经 `doc.graphrag` 回显（ok/method/entities/relationships 或 degraded/error）；③`POST /api/kb/{id}/graphrag-search`（③，§3.4 契约对齐；路由按既有 `/api/kb/{id}/` 前缀）+ `retrieval` 事件 `mode`/`degraded`（④）；④§3.1 双轨管道检索侧实现：graphrag KB 检索先 worker /query（claims→hits），失败回退向量检索（⑥ 降级语义）；⑤worker `/graphrag/ingest` 已实现（KB-O4 关闭）：semantica 抽取器不可用时内置轻量规则抽取兜底；⑥UI 双子页签 + mode 徽标 + KG 抽取状态徽标 + 一致性提示（⑦）；⑦冒烟 8 场景全通过（含降级/恢复重抽） | 董奎 |

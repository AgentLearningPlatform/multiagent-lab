---
module: 知识库
topic: 能力增强调研
desc: 对标主流产品的知识库与知识图谱产品能力增强调研（REQ-141）
synced: 2026-09-25
---

# 21_知识库与知识图谱产品能力增强调研（REQ-141）

> 状态：Draft v0.1（2026-09-24，REQ-141 调研交付）
> 作者：董奎 × 自动化执行者
> 需求锚点：11 v0.9 REQ-141（P3 调研）；结论供主人评估是否立项，**本文档不构成排期承诺**

## 1. 调研背景

REQ-141（2026-09-24 立项）指出当前知识库实现较单薄，要求对标主流产品补齐产品能力并产出立项建议。本调研对标四类主流产品：**RAGFlow 0.27**（深源文档理解）、**WeKnora 0.8**（微信开源混合检索）、**Dify 0.10**（LLMOps 知识库）、**微软 GraphRAG 1.x**（社区摘要全局问答基线），逐能力给出「本平台现状 → 差距 → 引入建议」。

## 2. 能力对照与建议

| 能力 | 主流做法 | 本平台现状 | 差距 | 建议 | 优先级 |
| --- | --- | --- | --- | --- | --- |
| 多格式摄入 | RAGFlow：PDF/Word/Excel/OCR + 版面分析（DeepDoc） | 仅粘贴/txt/md | 富格式是第一缺口 | 接 MinerU/unstructured 类解析前置服务（独立容器，不进 run-dev 主链路） | P2 |
| 切分策略 | RAGFlow 模板化切分；父子分块（small-to-big） | 固定 500/50 字符 | 固定切分伤语义 | ①标题层级感知切分（md 优先）；②父子分块（子块检索、父块召回上下文） | P2 |
| 混合检索 | WeKnora：BM25+向量+RRF 融合 | 纯向量（Qdrant/SQLite fallback） | 关键词/编号类查询召回差 | SQLite FTS5 BM25 + 向量 RRF 融合（k=60），零新依赖 | P2 |
| Rerank | Dify/RAGFlow：bge-reranker/Cohere | 无 | Top-K 精排缺失 | eino-ext rerank 组件接 bge-reranker（本地或 API），仅 top_n≤20 时不增太多延迟 | P3 |
| 查询改写 | Dify：多轮改写/HyDE | 无 | 多轮对话指代消解差 | 对话侧接入前用 LLM 改写（复用 REQ-98 能力代理），可选开关 | P3 |
| 图谱构建联动 | GraphRAG：实体→关系→claims→社区 全管线 | internal/kg 已有（REQ-110/127/128/129/130） | 抽取质量与规模受限于 60 chunk 预算 | 预算随库规模自适应；抽取治理（REQ-129）已就位 | 已闭环 |
| 图谱问答联动 | GraphRAG local/global 双通道 | local（三步检索）+ global（REQ-130 社区摘要）已并存 | 多跳深度限 2 跳 | 递归 CTE 扩到 3 跳可配；跨库联合检索 | P3 |
| FAQ 索引 | Dify FAQ 模式（问答对直建索引） | 无 | 高频问答场景效率低 | FAQ 文档类型（q+a 双字段双索引） | P4 |

## 3. 立项建议（供主人拍板）

1. **建议立项（P2）**：「混合检索 BM25+向量 RRF」+「父子分块」——零新外部依赖、直接提升召回质量，与现有 KB-2 池中的对应条目合并为一个迭代（约 2~3 天）。
2. **建议立项（P2，独立部署）**：「富格式摄入前置服务」——MinerU 容器 + 摄入管线接 tpl；**不违反 D-O15**（该服务是可选外挂、非 run-dev 必需链路，未安装时平台功能不变）。
3. **暂缓**：Rerank（等待真实召回质量反馈）、查询改写（成本敏感）、FAQ（场景驱动）。
4. **与 KB-3 关系**：以上若立项，KB-3（WeKnora/RAGFlow 独立服务集成，KB-O2）优先级下降——自研补齐后接入外部 RAG 的必要性降低（D-KB2 口径复核点）。

## 4. 迭代记录

| 版本 | 日期 | 变更 | 作者 |
| --- | --- | --- | --- |
| v0.1 | 2026-09-24 | 首版：REQ-141 调研交付——四产品对标 + 8 能力差距对照 + 立项建议 | 董奎 × 自动化执行者 |
